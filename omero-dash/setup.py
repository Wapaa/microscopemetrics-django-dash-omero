from setuptools import setup, find_packages

setup(
    name="omero-dash",
    version="0.1.0",
    description="This app is a visualization tool using django_plotly_dash.",
    packages=find_packages(),
    keywords=["omero"],
)
